apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: webserver
  name: webserver
spec:
  rules:
  - host: ${HOST_ING}
    http:
      paths:
      - backend:
          serviceName: webserver
          servicePort: 8080
        path: /
